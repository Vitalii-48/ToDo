Flask Task Manager

📌 Опис

Це веб-додаток на Flask для керування завданнями з підтримкою авторизації, ролей (user / admin), реєстрації, редагування та перегляду завдань. Проєкт реалізує базову логіку керування користувачами та завданнями через шаблони HTML та SQLite-базу.

📁 Структура проєкту

C:\PythonProjects\Flask/
│── .venv/                 # Віртуальне середовище
│── static/                # Статичні файли (CSS, JS, зображення)
│   ├── css/               # CSS стилі
│   │   └── style.css      # Основний файл стилів                # Статичні файли (CSS, JS, зображення)
│── templates/             # HTML-шаблони
│   ├── login.html          
│   ├── register.html
│   ├── tasks.html
│   ├── admin.html
│   ├── add_task.html
│   ├── edit_task.html
│   ├── index.html
│   └── welcome.html
│── app.py                 # Основний Flask-додаток
│── database.py            # Робота з SQLite
│── database.db            # SQLite-база даних
│── .env                   # Конфігурація середовища
│── .gitignore             # Ігнорування файлів для Git
│── requirements.txt       # Залежності

⚙️ Установка

Створити віртуальне середовище:

python -m venv .venv
source .venv/bin/activate  # або .venv\Scripts\activate на Windows

Встановити залежності:

pip install -r requirements.txt

Запустити додаток:

flask run

🔐 Авторизація

Користувач вводить username і password.

Якщо роль у базі — admin, перенаправлення на /admin_board.

Якщо роль — user, перенаправлення на /tasks.

Дані зберігаються в session.

🧠 Структура бази даних

Таблиця users: id, username, password, role

Таблиця tasks: id, user_id, description, created_at, due_at, status, priority, done, FOREIGN KEY


🛡️ Рекомендації

Використовувати werkzeug.security для хешування паролів.

Додати CSRF-захист через Flask-WTF.

Створити декоратори для перевірки ролей (@admin_required, @login_required).

📌 Автор

Розроблено Віталієм Івановим — Flask-розробником з фокусом на чисту архітектуру, симетрію шаблонів і maintainability.
Підтримка ШІ.