services:
  - type: web
    name: todo-flask
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: TD_SECRET_KEY
        generateValue: true
